#include <stdio.h>
#include <string.h>
#include "main.h"
/**
* _strlen - controls if a character is in lowercase
*@s: character
*Return: return the length
*/
int _strlen(char *s)
{
int l;
l = strlen(s);
return (l);
}
