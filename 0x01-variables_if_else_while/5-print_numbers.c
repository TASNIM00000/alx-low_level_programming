#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *Discription: 'num'
 * Return: Always 0 (Success)
 */
int main(void)
{
for(int i =0;i <=9; i++)
{
printf("%d", n);
}return(0);
}
