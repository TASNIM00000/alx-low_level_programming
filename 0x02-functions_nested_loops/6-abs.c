#include "main.h"
/**
* _abs - checkk if a character is in lowercase
*@n: number
*Return: return abs
*/
int _abs(int n)
{
int abs;
if (n > 0)
return (n);
else if (n < 0)
return (-n);
else 
return(0);
}
